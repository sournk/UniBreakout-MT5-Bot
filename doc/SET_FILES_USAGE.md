# Работа с SET-файлами в UniBreakout Bot

## 📋 Что такое SET-файлы?

SET-файлы - это стандартный формат MetaTrader 5 для сохранения параметров индикаторов и советников. Они содержат не только значения параметров, но и настройки для оптимизации в Strategy Tester.

## 🎯 Зачем использовать SET-файлы?

1. **Удобство** - настроили индикатор один раз, сохранили в SET-файл
2. **Переносимость** - один файл содержит все параметры
3. **Оптимизация** - в SET-файле хранятся параметры для оптимизации
4. **Универсальность** - работает с любыми индикаторами без изменения кода бота

## 📝 Формат SET-файлов

```
; Комментарий (строка начинается с точки с запятой)
ParameterName=Value||Start||Step||Stop||Optimize
```

**Поля:**
- `ParameterName` - имя параметра (должно совпадать с именем в индикаторе)
- `Value` - текущее значение
- `Start` - начальное значение для оптимизации (опционально)
- `Step` - шаг изменения при оптимизации (опционально)
- `Stop` - конечное значение для оптимизации (опционально)
- `Optimize` - флаг оптимизации: Y/N или 1/0 (опционально)

## 📂 Где хранить SET-файлы?

Поместите SET-файлы в одну из папок:
- `MQL5/Files/` - основная папка для пользовательских файлов
- `MQL5/Presets/` - папка пресетов MetaTrader
- `Tester/` - папка тестера стратегий

Парсер автоматически найдет файл в любой из этих папок.

## 🔧 Как использовать с UniBreakout Bot

### Вариант 1: С использованием SET-файла (Рекомендуется)

**Шаг 1:** Создайте SET-файл с параметрами индикатора

Файл: `MQL5/Files/MyIndicator.set`
```
; Moving Average Parameters
Period=14||10||2||50||Y
Shift=0
MAMethod=0||0||1||3||Y
AppliedPrice=1
```

**Шаг 2:** Настройте параметры бота

```
I_CUS_NAM = "MyCustomIndicator"      // Имя индикатора
I_CUS_SET = "MyIndicator.set"        // SET-файл с параметрами
I_CUS_PRM = ""                       // Оставьте пустым
I_CUS_BUP = 0                        // Номер буфера для покупок
I_CUS_BDN = 1                        // Номер буфера для продаж
```

### Вариант 2: Ручной ввод параметров

Если SET-файл не нужен, можно ввести параметры вручную строкой:

```
I_CUS_NAM = "MyCustomIndicator"
I_CUS_SET = ""                       // Оставьте пустым
I_CUS_PRM = "int:14;int:0;int:0;int:1"  // Формат: type:value;type:value;...
I_CUS_BUP = 0
I_CUS_BDN = 1
```

**Формат ручных параметров:**
```
type:value;type:value;...
```

Поддерживаемые типы:
- `int:` - целые числа (например: `int:14`)
- `double:` - дробные числа (например: `double:1.5`)
- `string:` - строки (например: `string:MyValue`)
- `bool:` - логические (например: `bool:true` или `bool:false`)

**Пример:**
```
"int:14;int:0;int:0;int:1"
// period=14, shift=0, method=0, price=1
```

## 📖 Примеры SET-файлов

### Пример 1: Custom Moving Average

Файл: `CustomMA.set`
```
; Custom Moving Average
; Parameters for breakout strategy
;
MAPeriod=14||10||2||50||Y
MAShift=0||0||1||5||N
MAMethod=0||0||1||3||Y
AppliedPrice=1||0||1||6||N
```

**Использование:**
```
I_CUS_NAM = "Examples\\CustomMA"
I_CUS_SET = "CustomMA.set"
I_CUS_BUP = 0
```

### Пример 2: Donchian Channel

Файл: `DonchianChannel.set`
```
; Donchian Channel Breakout
;
Period=20||10||5||50||Y
Shift=0||0||1||3||N
ChannelMode=0
```

**Использование:**
```
I_CUS_NAM = "DonchianChannel"
I_CUS_SET = "DonchianChannel.set"
I_CUS_BUP = 0  // Верхняя граница канала
I_CUS_BDN = 1  // Нижняя граница канала
```

### Пример 3: Pivot Points

Файл: `PivotPoints.set`
```
; Pivot Points Daily
;
PivotType=0||0||1||2||Y
Timeframe=16408
ShowHistory=true||false||false||true||N
DrawLevels=true
```

**Использование:**
```
I_CUS_NAM = "PivotPoints"
I_CUS_SET = "PivotPoints.set"
I_CUS_BUP = 1  // Resistance R1
I_CUS_BDN = 2  // Support S1
```

## 🔨 Как создать SET-файл

### Способ 1: Через MetaTrader 5 (Рекомендуется)

1. Откройте график
2. Перетащите нужный индикатор на график
3. Настройте параметры в диалоговом окне
4. Нажмите кнопку **"Сохранить"** (💾)
5. Введите имя файла и сохраните
6. Файл будет сохранен в `MQL5/Presets/`
7. Скопируйте его в `MQL5/Files/` если нужно

### Способ 2: Вручную в текстовом редакторе

1. Создайте файл с расширением `.set`
2. Сохраните в `MQL5/Files/`
3. Заполните по формату (см. выше)

**Шаблон:**
```
; Название индикатора
; Краткое описание
;
Parameter1=Value1||Start1||Step1||Stop1||Y
Parameter2=Value2
Parameter3=Value3||Start3||Step3||Stop3||N
```

## 🧪 Тестирование

Для проверки работы парсера SET-файлов запустите тестовый скрипт:

```
test/TestCSetFileParser.mq5
```

Скрипт:
1. ✅ Загружает SET-файл
2. ✅ Парсит все параметры
3. ✅ Конвертирует в MqlParam
4. ✅ Создает тестовый индикатор
5. ✅ Выводит подробный лог

**Запуск:**
```
1. Откройте TestCSetFileParser.mq5 в MetaEditor
2. Укажите имя SET-файла в параметре InpSetFileName
3. Нажмите F5 (Compile and Run)
4. Проверьте лог в окне "Experts"
```

## 🐛 Troubleshooting

### Проблема: "Не удалось открыть SET-файл"

**Решение:**
1. Проверьте имя файла (с расширением .set)
2. Убедитесь, что файл в правильной папке:
   - `MQL5/Files/`
   - `MQL5/Presets/`
3. Проверьте права доступа к файлу
4. Проверьте кодировку файла (должна быть ANSI или UTF-8)

### Проблема: "Ошибка конвертации параметров"

**Решение:**
1. Проверьте формат SET-файла
2. Убедитесь, что каждая строка имеет формат `Name=Value`
3. Комментарии должны начинаться с `;` (точка с запятой)
4. Проверьте, нет ли пустых значений

### Проблема: "Индикатор не создается"

**Решение:**
1. Проверьте имя индикатора в `I_CUS_NAM`
2. Убедитесь, что индикатор существует в папке `MQL5/Indicators/`
3. Проверьте, что количество параметров соответствует индикатору
4. Проверьте типы параметров (int, double, string, bool)
5. Проверьте порядок параметров (должен совпадать с порядком в индикаторе)

### Проблема: "Неправильные значения параметров"

**Решение:**
1. Убедитесь, что типы параметров определены правильно:
   - Целые числа: `14`, `-5`
   - Дробные: `1.5`, `0.01`
   - Логические: `true`, `false`
   - Строки: любой текст
2. Проверьте порядок параметров в SET-файле
3. Параметры должны идти в том же порядке, что и в индикаторе

## 💡 Советы и рекомендации

1. **Именование SET-файлов:**
   - Используйте понятные имена
   - Указывайте версию при необходимости
   - Пример: `MA_Period14_v1.set`

2. **Комментарии:**
   - Добавляйте описание параметров
   - Указывайте дату создания
   - Описывайте назначение настроек

3. **Оптимизация:**
   - Используйте флаг `Y` только для важных параметров
   - Не оптимизируйте все параметры сразу
   - Указывайте разумные диапазоны Start/Stop

4. **Версионирование:**
   - Сохраняйте разные версии SET-файлов
   - Используйте git для версионирования
   - Документируйте изменения

5. **Тестирование:**
   - Всегда тестируйте новые SET-файлы перед использованием
   - Проверяйте значения в логе
   - Убедитесь, что индикатор создается корректно

## 📚 Дополнительная информация

- [Документация по CSetFileParser](../Include/DKStdLib/Common/CSetFileParser.mqh)
- [Примеры SET-файлов](.)
- [Тест парсера](../test/TestCSetFileParser.mq5)

## ❓ FAQ

**Q: Можно ли использовать базовые индикаторы (iMA, iRSI)?**
A: SET-файлы работают только с iCustom. Для базовых индикаторов используйте ручной ввод параметров.

**Q: Сколько параметров можно передать?**
A: Неограниченно. Парсер поддерживает любое количество параметров.

**Q: Можно ли менять параметры без перекомпиляции?**
A: Да! Измените SET-файл и перезапустите бота. Параметры загрузятся автоматически.

**Q: Работает ли в Strategy Tester?**
A: Да, но файл должен быть в папке `MQL5/Files/` или скопирован в `Tester/`.

**Q: Можно ли использовать один SET-файл для разных индикаторов?**
A: Нет, каждый индикатор имеет свои уникальные параметры.

---

**Copyright 2026, Denis K.**
**Version: 1.0**
